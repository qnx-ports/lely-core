
#include <lely/co/type.h>

/**
 * Number of valid calls to co_val_read(), -1 means no limit.
 */
extern int override_co_val_read_vc;

/**
 * Number of valid calls to co_val_write(), -1 means no limit.
 */
extern int override_co_val_write_vc;
